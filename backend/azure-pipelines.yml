trigger:
  branches:
    include:
      - main
  paths:
    include:
      - backend/*

pool:
  name: my-vm-pool
  demands:
    - agent.name -equals myvm-agent

variables:
  imageName: 'myapp'
  acrName: 'teccitydemo.azurecr.io'
  tag: $(Build.BuildId)

stages:
- stage: Build
  jobs:
  - job: BuildAndPush
    steps:
    - task: Docker@2
      displayName: Build backend image
      inputs:
        command: build
        repository: $(acrName)/$(imageName)
        dockerfile: Dockerfile
        buildContext: .
        tags: |
          $(tag)
        containerRegistry: 'ACR-Registry-Connection'   # <-- Service connection

    - task: Docker@2
      displayName: Push backend image
      inputs:
        command: push
        repository: $(acrName)/$(imageName)
        tags: |
          $(tag)
        containerRegistry: 'ACR-Registry-Connection'

- stage: Deploy
  dependsOn: Build
  jobs:
  - job: DeployToWebApp
    steps:
    - task: AzureWebAppContainer@1
      displayName: Deploy to Web App
      inputs:
        azureSubscription: 'Azure-ARM-Connection'   # Your service connection
        appName: 'teccitydemo'                    # Same as created above
        imageName: 'teccitydemo.azurecr.io/myapp:$(tag)'

